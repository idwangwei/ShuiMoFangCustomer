<view class=" page">

    <view class="page__bd">
        <view class="weui-tab">
            <view class="weui-navbar">
                <block wx:for="{{tabs}}" wx:key="*this">
                    <view id="{{index}}" class="weui-navbar__item {{activeIndex == index ? 'weui-bar__item_on' : ''}}"
                          bindtap="tabClick">
                        <view class="weui-navbar__title">{{item.desc}}</view>
                    </view>
                </block>
                <view class="weui-navbar__slider"
                      style="left: {{sliderLeft}}px; transform: translateX({{sliderOffset}}px); -webkit-transform: translateX({{sliderOffset}}px);">
                </view>
            </view>

            <view class="weui-tab__panel">
                <!--我的推广-->
                <view wx:if="{{activeIndex == 0}}" class="share-data-box">
                    <view class="box-title">
                        <text class="title-item">邀请人</text>
                        <text class="title-item">获得积分</text>
                        <text class="title-item">推广时间</text>
                    </view>
                    <view wx:for="{{shareData}}" class="list-item-box">
                        <text class="list-item">{{item.to}}</text>
                        <text class="list-item">{{item.score}}</text>
                        <text class="list-item">{{item.date}}</text>
                    </view>
                </view>
                <!--推广海报-->
                <view wx:elif="{{activeIndex == 1}}">
                    推广海报
                </view>
                <!--推广规则-->
                <view wx:else>
                    规格规则
                </view>

            </view>
        </view>
    </view>
</view>